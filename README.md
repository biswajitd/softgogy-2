# Softgogy

Algo Trading Software

## Project info

**Live site:** https://biswajitd.github.io/softgogy/

**Edit this site:** http://127.0.0.1:8777/open/d5eb8dcaa31e

Open that address on the computer where WebForge is installed. It loads this
project, and you can change the theme, add or rename pages, edit the copy and
push the result straight back to this repository.

> WebForge runs on your own machine, so the link only works there. If the port
> was changed, use the address printed in the WebForge window instead.

## How can I edit this site?

**Use WebForge** — open the link above, make the change, press **Publish**. The
new version is committed to this repository automatically.

**Edit a file directly on GitHub** — open any `.html` file and press the
pencil icon, change it, then commit. The site is plain HTML, so there is
nothing to build.

**Work locally in your own editor**

```bash
git clone <YOUR_GIT_URL>
cd <YOUR_PROJECT_NAME>
python -m http.server 8000     # then open http://localhost:8000
```

Commit and push as usual. There is no build step and no dependencies to install.

**Built for:** Generic audience in the city

## Pages

- `index.html`
- `platforms.html`
- `5paisa.html`
- `angel-one.html`
- `dhan.html`
- `faq.html`
- `groww.html`
- `upstox.html`
- `zerodha.html`
- `refund.html`
- `software-features.html`
- `software-utilities.html`
- `contact.html`

## Run locally

No build step required - it is a static site.

```bash
python -m http.server 8000
# then open http://localhost:8000
```

## Deploy

**GitHub Pages:** Settings -> Pages -> Source: `Deploy from a branch` -> Branch: `main` / `root`.

**Netlify / Vercel / Cloudflare Pages:** drag the folder in, or point at this repo.
No build command, publish directory `/`.

## Stack

Static HTML + Tailwind CSS (CDN) + vanilla JavaScript. Dark mode, responsive, no dependencies.

---

Generated by WebForge from the brief:

> Pl make a website like www.softgogy.in
